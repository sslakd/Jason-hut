# ZomChess Board Assets

Replace any file in this folder with your own art using the same filename.

Recommended image shape: square, 256x256 or larger.

The renderer reloads these assets automatically about every 1.5 seconds in the browser, using cache-busted URLs.
